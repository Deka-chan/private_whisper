# Rust
/target/
**/*.rs.bk
Cargo.lock

# Model files (downloaded at runtime, never committed)
/models/
*.onnx
*.tar.bz2

# Local CUDA runtime libs (pip wheels for dev GPU testing in WSL2)
/.cudalibs/

# OS / editor
.DS_Store
*.swp
