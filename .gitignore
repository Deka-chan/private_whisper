# Rust
/target/
**/*.rs.bk

# Model files (downloaded at runtime, never committed)
/models/
*.onnx
*.tar.bz2

# Local CUDA runtime libs + GPU onnxruntime (dev GPU testing in WSL2)
/.cudalibs/
/.cudalibs13/
/.ortgpu/
/.winrt/
/dist/

# OS / editor
.DS_Store
*.swp

# Stray screenshots / scratch images (commit real assets with -f if needed)
*.png
